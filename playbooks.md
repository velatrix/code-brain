# Playbooks

When working on a project, check the row that matches and read the linked guide. Every session — not only at project start.

| Working on | Guide |
|---|---|
| **Frontend** (web app UI) | [[frontend-app-shell]] |

Add a row when a project type accumulates its own guide.
